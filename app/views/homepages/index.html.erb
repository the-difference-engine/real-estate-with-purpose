
  <head>
  </head>

<div class="list-content">
<div class="container">
  <h2> About Real Estate With Purpose</h2>
  <h4>Real Estate represents more than a financial transaction for four walls and a roof - it is an investment and personal commitment to Chicago's neighborhood and the surrounding community. To that end, 10% of my commission goes to reinvest in the community through the local charity and organization of my clients choice.
  </h4>
</div>
</div>

<div class="list-content">
<div class="container">
  <h2> Donation Calculator </h2>
  <h3> Total amount donated </h3>
  <br>

    <div class ="input-group-dc">
      <span class="input-group-addon">Enter your budget: $</span>
      <input type="text" class="form-control" id="budget">
      <span class="input-group-addon">See your donation amount! (USD)</span> 
      <input type="text" class="form-control" name="textbox" id="textbox" readonly="true">
     </div>

  <p>
      <% if @donations %>
      <% @donations.each do |donation| %>
      Charity: <%= donation.charity.name %>
  <br>
      Amount: <%= donation.amount_donated %>
      <% end %>
    <% else %>
      0 Donations
    <% end %>
  </p>
  <h3> Number of charities donated to </h3>
  <%= @donations.count %>
  <br>
  <h3> Average donation amount </h3>
    <% if @donation %>
      <%= @donation.average_donation %>
    <% else %>
      0 dollars donated
    <% end %>

</div>
</div>

<h2><span class="get-ready">READY TO GET STARTED?</span></h2>
<%= form_tag '/properties', method: :post do %>
  <div class="top-search-bar">
    <div class="form-item">
      <%= text_field_tag :location, nil, placeholder: "Address, neighborhood, zip", style: "width: 200px" %>
    </div>
    <div class="form-item">
      <%= text_field_tag :price_min, nil, placeholder: "Price (min.)"%>
    </div>
    <div class="form-item">
      <%= text_field_tag :price_max, nil, placeholder: "Price (max.)" %>
    </div>
    <div class="form-item">
      <%= text_field_tag :beds_min, nil, placeholder: "Beds (min.)" %>
    </div>
    <div class="form-item">
      <%= text_field_tag :beds_max, nil, placeholder: "Beds (max.)" %>
    </div>
    <div class="form-item">
      <%= text_field_tag :baths_min, nil, placeholder: "Baths (min.)"%>
    </div>
    <div class="form-item">
      <%= text_field_tag :baths_max, nil, placeholder: "Baths (max.)" %>
    </div>
    <div class="form-item">
      <%= submit_tag "Submit" %>
    </div>
  </div>
<% end %>
</div>
</div>
